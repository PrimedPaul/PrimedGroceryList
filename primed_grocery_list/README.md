Primed Grocery — Shopping List Manager

Quick start

1. Install Flutter SDK: https://flutter.dev/docs/get-started/install
2. From this project root run:

```bash
flutter pub get
flutter run
```

Notes

- Data is persisted locally using `shared_preferences`.
- Add items with the + FAB; tap checkbox to toggle bought; tap delete to remove.
