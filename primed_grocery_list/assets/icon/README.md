# App Icon

Place your app icon here as `app_icon.png`.

Requirements:
- File name: `app_icon.png`
- Size: **1024x1024 pixels** (minimum)
- Format: PNG with transparency supported
- No rounded corners needed — the OS applies masking automatically

After placing the image, run:
```
dart run flutter_launcher_icons
```
